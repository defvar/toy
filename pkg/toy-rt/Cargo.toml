[package]
name = "toy-rt"
version = "0.1.0"
authors = ["defvar <def.daisuke@gmail.com>"]
edition = "2021"

[dependencies]
tokio = { version = "1.19.2", features = ["rt-multi-thread", "sync", "time", "tracing"] }
