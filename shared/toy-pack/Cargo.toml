[package]
name = "toy-pack"
version = "0.1.0"
authors = ["defvar <def.daisuke@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
failure = "0.1.6"
toy-pack-derive = { optional = true, path = "../toy-pack-derive" }

[features]
# Provide derive(Pack, UnPack) macros.
derive = ["toy-pack-derive"]
